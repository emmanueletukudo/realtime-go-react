# Realtime Go + React

In this tutorial, you’ll learn what real-time systems are, why most web applications aren't designed as true real-time, and how to build a soft real-time system using tools you’re probably already familiar with: `Go`, `React`, and `TypeScript`. To set up everything correctly, follow the instructions in the `backend` and `frontend` folders carefully.
